# ---
# chapter: 1
# topic: 元组 Tuple — 不可变序列
# section: 1.3.4
# difficulty: ⭐⭐
# tier: core
# deps: []
# run: python 14_tuple_operations.py
# expected_runtime: <1s
# expected_output: 元组操作示例
# ---
# See: ../tutorial/01_Python编程基础.md (lines 789-862)
# Interview hooks:
#   1. t = (1, [2, 3]) 是不可变的吗?为什么?
#   2. tuple 的"不可变性"是深还是浅?
#   3. 单元素元组为什么必须加逗号?
"""
元组：不可变序列 — 面试陷阱最多的数据结构

核心考点：元组的"不可变"指的是元组对象本身不可变，
          但如果元组中包含可变对象，可变对象的内容可以修改！
"""

# ─────────────────────────────────────────────────────────────
# 🎯 面试陷阱：t = (1, [2, 3]) 的可变性辨析
# ─────────────────────────────────────────────────────────────

t = (1, [2, 3], {"a": 4})

# t[0] = 10        # TypeError — 不能修改元组元素
# t[1] = [5, 6]    # TypeError — 不能替换整个列表

t[1].append(4)     # ✅ 合法！修改的是列表内部，不是元组本身
print(f"修改后: {t}")           # (1, [2, 3, 4], {'a': 4})

t[2]["b"] = 5      # ✅ 合法！修改的是字典内部
print(f"再加一项: {t}")           # (1, [2, 3, 4], {'a': 4, 'b': 5})

"""
内存模型解析：

┌──────────────────────────────────────────┐
│  元组对象 (tuple)                         │
│  ┌─────────┬─────────┬─────────┐        │
│  │  ref 0  │  ref 1  │  ref 2  │        │
│  │  ────▶  │  ────▶  │  ────▶  │        │
│  │   1     │  [2,3]  │  {a:4}  │        │
│  └─────────┴─────────┴─────────┘        │
│       │          │           │           │
│       ▼          ▼           ▼           │
│     int 1    list对象      dict对象       │
│              (可变)         (可变)        │
│                                          │
│  元组的引用不可变，但引用的对象内容可变！     │
└──────────────────────────────────────────┘

结论：tuple 的不可变性是浅层的（shallow immutability）
"""

# ─────────────────────────────────────────────────────────────
# 元组的性能优势
# ─────────────────────────────────────────────────────────────

# 1. 元组比列表更省内存（因为不可变，无需过度分配）
import sys
lst = [1, 2, 3, 4, 5]
t = (1, 2, 3, 4, 5)
print(f"列表内存: {sys.getsizeof(lst)} bytes")   # 列表内存更大
print(f"元组内存: {sys.getsizeof(t)} bytes")     # 元组内存更小

# 2. 元组可作为字典键（列表不行）
coord_dict = {
    (0, 0): "原点",
    (1, 0): "x轴",
    (0, 1): "y轴",
}
print(f"坐标字典: {coord_dict[(0, 0)]}")

# 3. 元组拆包
point = (3, 4)
x, y = point           # 元组拆包
first, *rest = (1, 2, 3, 4, 5)  # 扩展拆包
print(f"x={x}, y={y}, first={first}, rest={rest}")

# 单元素元组的陷阱
t_single = (42,)       # ✅ 必须加逗号！
not_tuple = (42)       # ❌ 这是 int，不是 tuple
print(type(t_single))   # <class 'tuple'>
print(type(not_tuple))  # <class 'int'>

if __name__ == "__main__":
    print("OK")
