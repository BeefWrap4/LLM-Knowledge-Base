---
chapter: 99
topic: 库健康检查报告
difficulty: "N/A"
interview_frequency: 0
created: 2026-06-06
updated: 2026-08-05
tags:
  - 健康检查
  - 维护
  - 验收
aliases:
  - Health Report
  - 库诊断
---

# 99_库健康检查报告

> 当前快照：2026-08-05。本文只报告可复验事实；历史 CI、真实 API、GPU 和 Docker
> 结果不能代替当前工作区验收。

## 1. 仓库快照

| 指标 | 当前值 | 验证方式 |
|---|---:|---|
| 正文章节 | 54 | `01_*.md` 至 `54_*.md`，编号连续 |
| 学习分部 | 8 | [[00_目录索引]] |
| 代码运行分组 | 29 | `code/chNN_topic/` |
| Python 示例 | 433 | 158 core + 199 llm + 76 gpu |
| 稳定主题 ID | 433/433 | `code/TOPIC_MANIFEST.json`，唯一且路径可解析 |
| 有代码覆盖的规范章 | 45/54 | 清单中 `chapter` 去重 |
| 权威来源入口 | 54/54 | `docs/AUTHORITATIVE_SOURCES.md` |

54 章是语义学习边界；29 个代码目录是为批量运行保留的工程分组，两者不再强制一对一。
旧 40 章的 265 个核心 H2 主题已全部且仅迁移一次，详见 [[docs/RECHAPTERING_MAP]]。

## 2. 当前工作区验收

### 2.1 离线必过项

| 验收项 | 结果 | 范围与边界 |
|---|---:|---|
| Core strict runner | 157 PASS / 1 SKIP / 0 FAIL | 共 158；LightGBM 未安装，按契约跳过 |
| LLM strict runner | 176 PASS / 23 SKIP / 0 FAIL | 共 199；`LLM_MOCK=1` |
| GPU conditional runner | 32 PASS / 44 SKIP / 0 FAIL | 共 76；未开启 `--real-gpu` |
| 三层示例合计 | 365 PASS / 68 SKIP / 0 FAIL | 433/433 均被 runner 收集并给出终态 |
| pytest（非 GPU） | 344 PASS / 1 SKIP / 3 DESELECTED | 当前 Python 与本机依赖环境 |
| pytest（GPU marker） | 3 PASS | 为单元/安全契约验证，不是真实模型推理 |
| 章节叙事契约 | 54/54 PASS | 导航、目标、连续主节与固定章末闭环 |
| Markdown / Mermaid | 116 / 243，0 FAIL | 围栏、表格、公式、链接与 Obsidian 兼容风险 |
| WikiLinks | 56/56 resolved，0 broken | 目标文件和别名可解析 |
| Ruff / compileall | PASS | `code/` 全量静态检查与编译检查 |
| Docker build / 容器验收 | PASS | 重建 `llm-kb:latest`；健康检查和容器内第 1–9 阶段通过，真实 API 阶段按设计跳过 |

### 2.2 标准命令

```powershell
cd code
python scripts/normalize_chapter_narrative.py --check
python scripts/build_topic_manifest.py
python scripts/rebuild_catalog_docs.py --check
python scripts/verify_all.py
python -m pytest tests -q -m "not gpu and not slow"
python -m pytest tests -q -m gpu
python -m ruff check .
python -m compileall -q .
python scripts/run_all_examples.py --tier core --parallel 4 --timeout 60
$env:LLM_MOCK = "1"
python scripts/run_all_examples.py --tier llm --parallel 4 --timeout 180
python scripts/run_all_examples.py --tier gpu --parallel 4 --timeout 180
```

`verify_all.py` 的第 1–9 阶段必须通过；第 10 阶段只有在显式传入
`--real-api --confirm-real`、设置 `LLM_MOCK=0` 并选择 provider 后才会访问真实 API。

## 3. 结构与渲染门禁

以下任一条件会使标准验证返回非零：

1. 章节不是连续 Ch01–Ch54，或正文契约不完整；
2. WikiLink、本地 Markdown 链接或示例路径无法解析；
3. Markdown 围栏、标题、表格、MathJax、Callout 或 Mermaid 出现已知 Obsidian 风险；
4. 433 个示例中任一个缺 metadata、`main` guard、唯一 `topic_id` 或清单记录；
5. 54 章中任一章缺少 HTTPS 一手来源入口；
6. README、目录、思维导图、健康报告或生成器快照不一致；
7. CI 未固定 `LLM_MOCK=1`，或示例默认产生下载、端口、编译、云资源或付费 API 副作用；
8. 代表性 smoke 示例失败，或代码使用内置 `eval`/`exec`。

## 4. 条件验收边界

- 本次未使用真实 API，未下载模型，未启动云资源。
- GPU runner 的 PASS 只证明可在当前环境执行的路径；44 个 SKIP 仍需各自声明的平台、模型、外部服务或多卡条件。
- 历史上 2026-07-31 已通过 DeepSeek、bge/Redis/pgvector、RTX 5090 D 和 Docker 条件验收；
  该结果只适用于当时提交与环境。

## 5. 维护规则

- 模型、SDK、benchmark、价格和法规必须注明核验日期及权威来源。
- 新增或移动示例时先选择语义 `topic_id`，再更新清单；不得仅依赖章节号。
- 修改章节边界后必须同步来源台账、WikiLink、代码归属和入口文档。
- 真实 API/GPU/Docker 结果必须记录版本、硬件、模型、数据和统计口径，不得用 mock 结果代替。

## 6. 历史

- 2026-07-31：完成 40 章事实、API、安全和可运行性审校，建立 fail-closed 验收。
- 2026-08-04：统一旧 40 章叙事结构，修复 Obsidian、Markdown、Mermaid、表格和公式渲染风险。
- 2026-08-05：按语义学习边界重构为 54 章、8 大部分；引入稳定主题 ID、迁移台账和幂等生成器。

历史提交或 workflow 的成功只说明对应提交和环境通过；当前状态永远以本节命令重新验证。
