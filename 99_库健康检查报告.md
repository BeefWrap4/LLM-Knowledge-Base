---
chapter: 99
topic: 库健康检查报告
difficulty: "N/A"
interview_frequency: 0
created: 2026-06-06
updated: 2026-07-31
tags:
  - 健康检查
  - 维护
  - 验收
aliases:
  - Health Report
  - 库诊断
---

# 99_库健康检查报告

> 当前快照：2026-07-31。本文只报告可复验事实，不使用永久健康分或手工维护的
> “100/100”。远端历史 CI、真实 API 和 GPU 结果不能替代当前工作区验收。

## 1. 仓库快照

| 指标 | 当前值 | 验证方式 |
|---|---:|---|
| 正文章节 | 40 | `01_*.md` 至 `40_*.md` |
| 有代码伴侣的章节 | 29 | `code/ch01_*` 至 `code/ch29_*` |
| Python 示例 | 433 | 158 core + 199 llm + 76 gpu |
| 章节 README | 29/29 | `code/ch*/README.md` |
| 权威来源条目 | 40/40 | `docs/AUTHORITATIVE_SOURCES.md` |
| 标准验收阶段 | 10 | `python code/scripts/verify_all.py`；离线模式下第 10 阶段按设计 SKIP |

Ch30–40 是正文专题，当前没有独立代码伴侣。README、目录索引和思维导图必须明确区分
“40 章正文”与“29 章代码伴侣”。

## 2. 验收分层

### 2.1 离线必过

```bash
cd code
python -m ruff check .
python -m pytest tests -q
python scripts/verify_all.py
python scripts/run_all_examples.py --tier core --parallel 4 --timeout 60
$env:LLM_MOCK = "1"  # PowerShell
python scripts/run_all_examples.py --tier llm --parallel 4 --timeout 180
python scripts/run_all_examples.py --tier gpu --parallel 4 --timeout 180
```

离线验收不得调用真实 LLM。`run_all_examples.py --tier llm` 默认补上
`LLM_MOCK=1`；`gpu` tier 默认附加 `--mock`，只能证明 mock/结构分支；`verify_all.py`
只有显式传入 `--real-api --confirm-real` 且 `LLM_MOCK=0` 才运行 provider doctor。

### 2.2 2026-07-31 本地实测

| 验收项 | 结果 | 解释 |
|---|---:|---|
| Core strict runner | 157 PASS / 1 SKIP / 0 FAIL | 共 158；LightGBM 未安装，示例按契约 `[SKIP] + OK` |
| LLM strict runner | 176 PASS / 23 SKIP / 0 FAIL | 共 199；强制 `LLM_MOCK=1`，可选依赖/真实服务分支按契约跳过 |
| GPU mock runner | 32 PASS / 44 SKIP / 0 FAIL | 共 76；未使用 `--real-gpu` |
| 三层示例合计 | 365 PASS / 68 SKIP / 0 FAIL | 433/433 均被 runner 收集并给出终态 |
| pytest（非 GPU） | 304 PASS / 1 SKIP | 唯一跳过项要求 Python 3.13+；当前为 Python 3.12 |
| pytest（GPU marker） | 3 PASS | GPU 专项测试独立执行 |
| Ruff | PASS | `python -m ruff check .` |
| Python 编译 | PASS | `python -m compileall -q` 覆盖 shared 与 Ch01–Ch29 |
| 十阶段验证 | 1–9 PASS / 10 SKIP | 40/40 章节、29/29 README、433/433 示例、40/40 来源、Wiki 0 断链；真实 API doctor 离线跳过 |

### 2.3 2026-07-31 条件验收实测

| 验收项 | 结果 | 实测范围 |
|---|---:|---|
| DeepSeek 最小真实冒烟 | PASS | `deepseek-v4-flash`；1.18 秒；103 tokens；精确返回 `OK` |
| 四组件真实集成 | 4/4 PASS | 本地 bge-small-zh-v1.5（512 维）、Redis 7.4.9、pgvector、自选 DeepSeek；19.3 秒 |
| RTX 5090 D 分章 GPU runner | 47 PASS / 29 SKIP / 0 FAIL | Ch16/19/21/24/25/26/28，共 76；串行、按章、离线权重模式 |
| Docker build | PASS | Python 3.12；官方 PyPI；本地验收标签 `llm-kb:validation-20260731`，约 137.5 MB |
| Docker 容器内十阶段验证 | 1–9 PASS / 10 SKIP | 镜像包含根教程、来源台账和 `code/`；真实 API 仍独立验收 |
| Compose Redis/pgvector | PASS | 隔离项目名；`16379` / `15432`；两服务 health 均为 healthy |
| DeepSeek 当日账号聚合 | ¥0.02 | UTC 2026-07-31：41 请求、22,290 tokens；控制台不提供单请求拆分 |

GPU 的 29 个条件跳过覆盖 Apple Silicon、llama.cpp、Ollama/WebLLM、大模型下载、
vLLM/TensorRT-LLM、常驻端口、云部署和多卡条件。它们不是失败，也不能宣传为对应
外部运行时已通过。真实分布式吞吐、目标模型精度/容量、远端 GitHub Actions 和发布标签
仍需在各自环境中完成。

### 2.4 条件验收规则

- GPU：`--real-gpu` 只关闭默认 mock；外部服务、下载、编译、浏览器和云路径还必须
  使用脚本声明的独立环境开关。
- 真实 API：显式设置 `LLM_MOCK=0`、`LLM_PROVIDER=<name>` 和对应本地 Key，再运行
  `python scripts/verify_all.py --real-api --confirm-real` 或专用工作流。
- Docker/Redis/pgvector：使用 integration workflow；不能由普通 pytest 结果代替。

## 3. Fail-closed 门禁

标准验证在以下任一情况返回非零：

1. Wiki 链接断裂；
2. 正文章节不是连续 Ch01–Ch40；
3. 代码章节、示例数或 README 数与快照不一致；
4. 章节编号重复，或示例缺 metadata/main guard；
5. code `# section:` 指向不存在章节，或覆盖率跌破已声明基线；
6. 40 章权威来源索引不完整；
7. README、索引、思维导图和本报告的当前统计不一致；
8. CI 未设置 `LLM_MOCK=1`；
9. GPU mock 示例未显式跳过或声明并通过本地安全审计；
10. 示例出现内置 `eval`/`exec`，或代表性 smoke 示例失败。

章节覆盖率衡量“至少有一个代码示例的主节比例”，不是内容质量分。当前代码伴侣只覆盖
Ch01–Ch29，因此该数字用于防回归，不能宣传成“教程 100% 代码覆盖”。

## 4. 内容维护规则

- 模型、SDK、benchmark、价格和法规必须注明核验日期及权威来源。
- 厂商未公开的参数量、训练硬件、内部路由和架构统一写“官方未公开”。
- benchmark 必须同时写模型版本、数据集、硬件、batch、精度和统计口径。
- 示例不得把模型输出直接传给 `eval`/`exec`，不得在默认 CI 中访问真实 API。
- 算术工具必须解析受限语法；空 `__builtins__` 的 `eval` 不是安全沙箱。
- 新章节先通过编号、引用、来源、代码契约和离线测试，再更新入口文档统计。

## 5. 历史记录

- 2026-06：建立 29 章代码伴侣、分层依赖、CI 与 Docker 工作流。
- 2026-06：新增 Ch25–39 等专题。
- 2026-07：新增 Ch40 国内岗位实战并加强 RAG/Agent 的评测与可靠性内容。
- 2026-07-31：完成事实、API、安全、结构和可运行性审校；统计统一为
  40 章、29 个代码章节、433 个示例，并将验证改为 fail-closed。
- 2026-07-31：补做 Docker、DeepSeek、Redis、pgvector 和 RTX 5090 D 条件验收；
  修复 Docker 软件源/构建上下文、Compose 隔离与端口、Flash causal 基线，以及
  GPU 外部副作用门禁。

历史提交或 workflow 的成功只说明对应提交和环境通过；当前状态永远以本节命令重新验证。
