# 历史实现方案（非当前教程规范）

本目录保存 2026-06-07 的设计稿与实施计划，用于追溯当时的决策，不是当前模型、SDK、依赖、
硬件或验收结果的来源。其中的型号、下载体积、路径、命令和预计结果可能已失效。

当前使用顺序：

1. 章节正文、根 `README.md` 与 `00_目录索引.md`；
2. `docs/AUTHORITATIVE_SOURCES.md` 中截至核验日期的权威来源；
3. `code/README.md`、`code/docs/` 和当前脚本的 `--help`；
4. `99_库健康检查报告.md` 中最近一次实际验收结果。

如需复用本目录中的方案，必须先对照当前实现与官方文档重新验证；不得把计划中的性能、模型
下载、真实 API/GPU/Docker 结果当作已经完成的证据。
